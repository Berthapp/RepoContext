# Sample C# App

A minimal ASP.NET-style project used as a RepoContext fixture. It contains
services, interfaces, controllers ([ApiController] routes), auth logic, tests
using the `<Name>Tests` convention, and an `appsettings.Production.json` with a
sensitive marker string that must never be indexed.
